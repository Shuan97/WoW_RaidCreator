<UserControl x:Class="WoW_RaidCreator.Views.MenuPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WoW_RaidCreator.Views"
             xmlns:uiModels="clr-namespace:WoW_RaidCreator.UI_Models"
             xmlns:woWRaidCreator="clr-namespace:WoW_RaidCreator"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="200">
    <StackPanel Background="BlueViolet">
        <Canvas Background="DarkMagenta" Height="120"/>
        <uiModels:MenuButton SetText="Players"/>
        <uiModels:MenuButton SetText="ICC25"/>
        <uiModels:MenuButton SetText="{Binding MenuPanel.Title}"/>
        <uiModels:MenuButton SetText="{Binding MenuPanel.Title}"/>
    </StackPanel>
</UserControl>
